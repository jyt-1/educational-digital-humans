<!-- [工单18] 人工智能NLP-Agent数字人项目-教育智能体 —— 项目进度与交接记录（覆盖工单16~20） -->

# 项目进度记录

> 每次收工或跨天续接时更新本文件。次日开工先读这里，再读 `CLAUDE.md`。
> 最后更新：**2026-09-17**

---

## 一、当前状态一句话

**工单16 已完成；工单17、工单18 代码全部交付（后端 + 前端均已提交），等浏览器验收与截图留证。**
服务已起、演示数据已就绪，可直接验收：**工单17 → 工单18 → 再开工工单19**。

---

## 二、工单进度总览

| 工单 | 内容 | 状态 |
| --- | --- | --- |
| 16 | 需求分析与功能设计（纯文档） | ✅ 完成，已提交 `d8e3241` |
| 17 | 智能备课 | 🔶 代码交付完毕，**待验收 + 截图** |
| 18 | 智能助教（多模态 RAG） | 🔶 代码交付完毕，**待验收 + 截图** |
| 19 | 个性化学习推荐 | ⬜ 未开始（依赖工单17 的题目表） |
| 20 | 面试 AI 复盘 | ⬜ 未开始（开工前需装 faster-whisper） |
| 阶段二 | 2D 形象 + TTS + provider 抽象层 | 🚫 锁定：工单16~20 全部验收前禁止开始 |

---

## 三、工单17 交付物

### 后端（`f2dc8be`）

- 认证模块：`app/auth.py`（bcrypt + JWT）、`app/api/auth.py`，角色 `teacher` / `student`，admin 绕过角色校验
- 备课 6 张表：`teaching_plans` / `plan_versions` / `coursewares` / `exercises` / `exam_questions` / `resources`
- SSE 流式生成：`POST /api/lesson/generate`，事件 `start` / `delta` / `done` / `error`
- 保存 / 列表 / 详情 / 版本列表 / 回滚 / 导出（docx、pptx）
- 导出文件名走 RFC 5987 编码（中文不乱码）
- **回滚是追加新版本，不删除历史**（`plan_versions` 只增不减）

### 前端（`ddbcf5e` + `ef3e464`）

- `views/lesson/Generate.vue` —— 生成表单 + 流式渲染框，逐字追加、可中断
- `views/lesson/Plans.vue` —— 列表、按类型/课程名筛选、行内导出
- `views/lesson/Edit.vue` —— 教案/案例 Markdown 编辑预览、课件逐页编辑、版本时间线回滚、导出
- `components/QuestionEditor.vue` —— 习题/试题逐题结构化编辑
- `api/lesson.js` —— SSE 用 `fetch` + `ReadableStream` 手写帧解析，**不用 axios**（XHR 拿不到分块响应）

### 测试

`backend/tests/pytest_工单17_智能备课.py` —— **57 passed**（9 个测试类）
运行：`cd backend && python -m pytest -q`

---

## 四、工单18 交付物（智能助教 · 多模态 RAG）

### 后端（`3746f96` + `03f49ae`）

- 4 张表：`kb_docs` / `kb_chunks` / `conversations` / `messages`（对齐设计文档第三章 DDL）
- **多模态解析器**（`app/services/parsers/`）：pdf（PyMuPDF，含 `find_tables` 与图片抽取）、
  docx（python-docx，含 OMML 公式与内嵌图）、pptx（含表格与图片）、xlsx、image、
  legacy（老 `.doc/.ppt/.xls` 走 UTF-16LE 文本抽取并在正文注明降级说明，**不静默缩窄格式支持**）
- 切块：约 500 字、重叠 80；表格与图片作为原子块不切分，超大表格分组并重复表头
- 入库流水线：上传 → 后台解析 → 切块 → 向量 → 状态流转（pending/parsing/done/failed，失败原因落库）
- 检索：向量召回 + BM25（jieba 分词）→ RRF 融合 → 可选云端重排（失败自动降级，永不阻断）
- 问答：SSE（`sources` → `delta`* → `done`/`error`），答案内嵌 `[n]` 角标，引用随答案落库
- 知识库隔离三重保障：collection 物理隔离（`public` / `u_{user_id}`）+ 接口层 owner 校验 +
  公共库仅教师可写（学生上传公共库返回 403）

### 前端（`0231609` + `51410ed`）

- `views/assistant/Knowledge.vue` —— 公共/私有库分页、拖拽上传带进度、解析状态自动轮询、
  详情抽屉展示块类型统计与内容块预览（图片块直接回显）
- `views/assistant/Chat.vue` —— 会话侧栏 + 流式回答，`[n]` 角标可点击定位引用卡片，可中断生成
- `views/assistant/Search.vue` —— 检索调试页，展示召回通道（vector / keyword / rerank）与重排开关
- `components/CitationList.vue` —— 引用卡片（问答页与检索页共用），支持查看原文与图片预览
- `api/sse.js` —— 通用 POST + SSE 工具，**未改动已验收的 `lesson.js`**

### 测试

`backend/tests/pytest_工单18_智能助教.py` —— 全量 **101 passed**（工单17 的 57 条 + 工单18 的 44 条）

### 演示数据（已就绪，无需重跑）

`backend/scripts/seed_kb.py` 已往**公共知识库**灌入 5 份人工智能课程资料（走真实上传接口）：

| 文件 | 块数 | 多模态构成 |
| --- | --- | --- |
| 人工智能导论-第三章-梯度下降与反向传播.docx | 5 | 正文 3 / 表格 1 / 图片 1 |
| 人工智能导论-第二章-机器学习基础.pdf | 3 | 正文 1 / 表格 1 / 图片 1（表格在**第 3 页**） |
| 机器学习实验指导-第一讲.pptx | 3 | 正文 1 / 表格 1 / 图片 1 |
| 期末知识点分值分布.xlsx | 2 | 表格 2 |
| 神经网络结构示意图.png | 1 | 图片 1 |

样本原件另存于 `data/samples/`（gitignore），可手工拖拽上传演示。

---

## 五、验收状态（DoD 五条逐条对照）

| # | 要求 | 状态 |
| --- | --- | --- |
| 1 | 功能按验收标准可现场演示 | ✅ 工单17 已用真实 DeepSeek 跑通全链路；工单18 已跑通（问答由 DeepSeek 真实回答，检索为本地向量） |
| 2 | pytest 全绿，覆盖核心接口 | ✅ 全量 101 passed（工单17：57 / 工单18：44） |
| 3 | 新增源文件头注释含工单编号 | ✅ 已逐个自检 |
| 4 | git 提交带 `[工单XX]` 前缀 | ✅ |
| 5 | `docs/evidence/工单XX/` 有截图或录屏 | ❌ **工单17、工单18 均待用户补** |

> 验收指引（起服务命令、URL、点哪些按钮）见交付时的对话记录；核心路径：
> 输入 http://localhost:5173 → 用 `demo_teacher` / `demo123456` 登录 →
> **工单17**：智能备课 → 生成教案 → 逐字流式 → 编辑页改字 → 保存（生成新版本）→ 回滚 → 导出 docx / pptx；
> **工单18**：知识库（5 份资料已在公共库）→ 智能问答提问「表格里 Adam 优化器适合什么场景？」
> → 看 `[n]` 角标与引用卡片（第 3 页表格）→ 检索调试页看召回通道。

---

## 六、下次开工第一步

**等用户浏览器验收工单17、工单18 并截图**；验收通过后开工 **工单19 个性化学习推荐**。

工单19 开工要点（详见 CLAUDE.md §8 工单19 一节）：

- 表：`knowledge_points`（prereq_id 自关联成图谱，内置「人工智能导论」40+ 知识点）、
  `questions`（复用工单17 生成的题）、`attempts`、`student_profile`（mastery 0~1）、`mistake_book`
- 画像：按知识点加权正确率 + 时间衰减（7 天内 1.0 / 30 天 0.7 / 更早 0.4）；支持导入历史成绩初始化
- 路径推荐：mastery < 0.6 的薄弱点沿图谱上溯到最先修薄弱点，输出顺序 + 可解释理由
- 自适应练习：连对 3 题升难度档，答错降档；AIGC 错题本（解析 + 归因 + 2~3 道变式题）
- 前端 `/learn`：掌握度雷达图 + 学习路径时间线 + 练习页 + 错题本
- 工单19 的题来自工单17 的试题生成，**顺序不可颠倒**

---

## 七、环境与服务现状（2026-09-17 收工）

- 后端 uvicorn 与前端 vite dev server 仍在后台运行（端口 8000 / 5173）。
  次日如端口被占用，先按 PID 结束：
  ```bash
  netstat -ano | grep ":8000" | grep LISTEN     # 取最后一列 PID
  taskkill //F //PID <PID>
  ```
- `.env` 现状（**与昨天不同，注意**）：
  - `LLM_API_KEY` = DeepSeek，实测连通（`/api/health` 返回 `llm_configured: true`）
  - `EMBEDDING_PROVIDER=local` —— 硅基流动的 `EMBEDDING_API_KEY` / `RERANK_API_KEY` 仍是占位符
    `sk-xxx`，**没有云端 key，检索走本地 `BAAI/bge-small-zh-v1.5`（512 维，CPU）**
  - 拿到云端 key 后切回 `EMBEDDING_PROVIDER=api` 即可，但**必须删掉 `data/chroma` 重新解析**
    （bge-m3 是 1024 维，与本地 512 维不通用；向量库签名文件会主动拦截并提示）
  - `RERANK_ENABLED=false`（开发期默认；验收前若要演示重排，需配 `RERANK_API_KEY` 再置 true）
- ⚠️ DeepSeek Key 曾在对话中粘贴过，**建议在控制台吊销后换新**；换完只改 `.env`，不要动 `.env.example`
- 开发库 `data/edu_agent.db` 内留有 **5 条真实 LLM 生成的备课记录**，便于演示：

  | id | 类型 | 版本 | 来源 |
  | --- | --- | --- | --- |
  | 1 | 教案 | v3（AI生成 → 教师编辑 → 回滚自v1） | 开发期验证，**版本链完整，回滚演示首选** |
  | 2 | 课件 | v1 | 开发期验证（导出 pptx 用） |
  | 5 | 习题 | v1（23 题，总分 80） | 开发期验证（修 score 缺陷后） |
  | 7 | 试题 | v1（23 题，总分 100） | 开发期验证（修总分缺陷后） |
  | 8 | 教案 | v2（AI生成 → 教师编辑） | **用户 2026-09-16 在浏览器实测生成并编辑** |

  > id=8 是用户走 UI 表单生成的（标题不含章节号），证明浏览器端
  > SSE 流式生成 → 在线编辑 → 保存 全链路已跑通，是验收的有力佐证，**不要清理**。
  > 注意：清理过记录后 SQLite 会复用最大 id+1，新记录可能拿到旧编号，别按 id 认人。
  如需清空重来：停后端 → 删 `data/edu_agent.db` → 重启（会重新建表，但**需要重新注册账号**）
- 工单18 演示数据：公共知识库 **5 份文档**（见第四节表格，均已 `done`）、
  问答会话 **1 条**（「表格里 Adam 优化器适合什么场景？」，带 5 条引用，可用于演示引用落库与历史会话）
- 测试账号：`demo_teacher` / `demo123456`（教师）
- 本机 Python 环境变了（2026-09-17）：`scikit-learn 1.9.1` / `scipy 1.18.1` / `pandas 3.0.5`
  由 conda 版升到 pip 版（原因见下方坑 8）。`torch 2.13.0+cpu` 未动，**仍是 CPU 版**。

---

## 八、已解决的坑（避免重复踩）

1. **`pytest.ini` 中文注释在中文 Windows 崩** —— iniconfig 按系统 GBK 读 .ini，报 `UnicodeDecodeError`。
   配置统一写在 `backend/pyproject.toml`（TOML 按 UTF-8 解析）。已写入 CLAUDE.md §9。
2. **`python_files` 必须含 `pytest_*.py`** —— CLAUDE.md 硬约束 3 规定的文件名不匹配 pytest 默认的 `test_*.py`，
   不配置会「no tests ran」。
3. **习题模板原先不要求 `score` 字段** —— 实测 23 道题分值全为 `None`，教师得手工补。
4. **试题模板手写「总分 100 分」但分项相加只有 90** —— 已改为由 `DEFAULT_EXERCISE_SPEC` / `DEFAULT_EXAM_SPEC`
   经 `_spec_line()` 生成题量与总分，文案与分值无法再脱节。上面 3、4 两条都有 `TestPromptSchema` 用例守着。
5. **`EventSource` 只支持 GET** —— 生成接口是 POST + JSON body，故前端用 `fetch` + `ReadableStream`
   手写 SSE 帧解析，同样逐块消费。理由写在 `frontend/src/api/lesson.js` 顶部注释。
6. **Python 读写文件一律显式 `encoding="utf-8"`** —— 连临时分析脚本也一样，否则中文 Windows 上默认 GBK 直接报错。
7. **重启后端要按 PID 杀** —— `taskkill` 按窗口标题过滤常常杀不掉，旧进程仍占 8000，
   新进程静默启动失败（`error while attempting to bind`），结果调试的是旧代码，白折腾一轮。
8. **numpy 2.x 与 conda 编译的 scipy/sklearn/pandas 不兼容**（工单18 期间修复）——
   `import sklearn` 直接 `ImportError: numpy.core.multiarray failed to import`，
   连 `sentence_transformers`、`transformers`（`generation.utils` 里 import sklearn）都被拖垮。
   修法：`pip install -U scipy scikit-learn pandas -i https://pypi.tuna.tsinghua.edu.cn/simple`
   （装 numpy 2 版轮子覆盖 conda 版，**不要降级 numpy**，很多新包已要求 numpy>=2）。
   副作用：`gensim 4.3.3` 声明 `numpy<2`，本就用不了，本项目不依赖它。
9. **本地 Embedding 不走 sentence-transformers** —— 依赖链长（sklearn/scipy/pandas），
   改为 `transformers` + `torch` 手写 CLS 池化 + L2 归一化，实测与官方实现数值一致（差异 3e-8）。
   代码在 `app/services/embedding.py::_embed_local`，切换只改 `.env` 的 `EMBEDDING_PROVIDER=local`。
10. **换 Embedding 模型必须重建向量库** —— 云端 bge-m3 是 1024 维、本地 bge-small 是 512 维，
    Chroma 的 collection 维度建库即固定。已加 `.embedding_signature.json` 签名文件：
    写入前拦截并给出「删 data/chroma 重新解析」的提示，召回侧自动降级为关键词召回。
11. **Vite 代理目标必须写 `127.0.0.1` 而不是 `localhost`**（工单18 期间修复）——
    Vite 监听 `[::1]:5173`，Node 把 `localhost` 解析到 IPv6 的 `::1`，
    而 uvicorn 默认只监听 IPv4 `127.0.0.1:8000`，结果前端所有 `/api` 请求 502。
12. **本机 shell 有系统代理**（`HTTP_PROXY=http://127.0.0.1:7897`）——
    用 curl 测本地端口会得到代理返回的 **502**，看着像服务挂了其实是没连上。
    本地自测加 `curl --noproxy '*'`，python 脚本用 `httpx.Client(trust_env=False)`。
13. **PyMuPDF 抽的表格/文本会带 HTML 实体**（`&amp;#45;` 之类）——
    解析器里统一 `unescape_entities()` 清洗；测试用例也据此断言。
14. **公式识别是保守启发式** —— 只把「无中文 + 含数学符号 + 运算符密度达标」的行判为公式块，
    纯 ASCII 公式（`y = wx + b`）按正文入库（仍可被检索到），宁可漏判不误判。
