<!-- 09-22 当日工作计划。原标注为「临时工作文件，不进 git」，2026-09-24 经拍板改为留档入库，
     它记录的三项结果（工单20/21/22 取证、文档漂移、演示动线）已由 f7bdc82 及后续提交落实。 -->
# 今日计划 · 2026-09-22

> 生成时间：14:15 ｜ 生成依据：git log / 工作区状态 / docs/evidence 实盘 / capture_evidence.py 场景清单 / 服务实探
> 核心判断：**代码已交付到工单22，唯一还硬着的空洞是"交付证据"**。今天补它。

---

## 一、今日目标

**把工单 20 / 21 / 22 的验收证据补齐，让 DoD 第 5 条不再是缺口；顺带清掉 3 处文档漂移。**

一句话验收：`docs/evidence/` 下 `工单20`/`工单21`/`工单22` 三个目录各有截图，断言全绿；
全仓 grep 不再有指向已删文件 / 已退役实现的描述。

---

## 二、开工前置（已完成，直接用）

- [x] 后端已起：`http://127.0.0.1:8000`，实测 **`/openapi.json` 路由 = 50**
      （阶段一 43 + TTS 3 + lecture 4）—— ⚠️ 进度记录里写的 **46 已过时**，别拿它当判据
- [x] 前端已起：`http://localhost:5173`（实测 200）
- [x] 工作区干净，`master` 最新 `3b252df`
- [x] 测试基线：**324 passed**（今天上午实测）

> 服务是我起的，**两个都没带 `--reload`/常驻保护**，中途若退出，按下面重启（各开一个终端）：
> ```bash
> cd C:/Users/23772/sx/Education-agent/backend && D:/anaconda3/python.exe -m uvicorn app.main:app --reload --port 8000
> cd C:/Users/23772/sx/Education-agent/frontend && npm run dev
> ```

---

## 三、P0 · 主线：补齐三个工单的交付证据（约 2~3 小时）

**现状（刚核对，不是印象）：**

| 目录 | 现状 |
| --- | --- |
| `docs/evidence/工单20/`（形象库） | **空目录，只有 `.gitkeep`** |
| `docs/evidence/工单21/`（虚拟教室·成课） | **目录不存在** |
| `docs/evidence/工单22/`（沉浸式助教台） | **目录不存在** |
| `backend/scripts/capture_evidence.py` | 场景清单**停在 `stage_avatar_speech`**（阶段二口径），无形象库切换、无助教台 25-75 舞台、无 ASR 聆听态、无虚拟教室翻页 |

工单 20/21/22 交付时都**只做了功能、没做机器取证** —— 阶段一那套"边点边断言边截图"的规矩，这三个工单没跟上。

- [ ] **1.1** 新增 `stage_avatar_library`：形象库切换 + 声音配对（女脸配女声）
      断言：切 3 个形象 → 请求体 `avatar_id` 跟随、`voice` 跟随
- [ ] **1.2** 新增 `stage_assistant_desk`：助教台 25-75 舞台 + ASR 聆听态
      断言：`[n]` 角标点击→引用抽屉自动展开并高亮；点麦克风→胶囊变「倾听中」
- [ ] **1.3** 新增 `stage_lecture_room`：虚拟教室翻页 + 字幕
      断言：`video.timeupdate` 命中 timeline → 页文本变化、字幕跟随
- [ ] **1.4** 跑一轮完整取证（先清空目标目录，保证编号连续），产出落 `工单20/21/22`
- [ ] **1.5** 跑完**还原演示库**（`cp data/edu_agent.db.bak-demo data/edu_agent.db`）——取证会写库，这条不能省

**完成定义**：三目录各有 ≥2 张 PNG、断言 100% 通过、控制台无 JS 错误、无 4xx/5xx。

> 注：ASR 聆听态是 Web Speech API，无头浏览器拿不到麦克风——这段只能断言「UI 状态切换」（胶囊文案 / 波形激活），
> 真识别效果归你现场验。这点我写进脚本注释，不假装机器验过。

---

## 四、P0 · 文档漂移 3 处（约 15 分钟）

**⚠️ 涉及的 `CLAUDE.md` 与 `docs/设计文档-工单16.md` 是承诺性文档——动之前先点头。**

| # | 位置 | 现在写的 | 实际 |
| --- | --- | --- | --- |
| 1 | `CLAUDE.md:91` 目录树 | `components 含 AvatarStage.vue` | 该文件 **09-18 已删**，现为 `AvatarSpotlight.vue` |
| 2 | `CLAUDE.md:273` 新增文件清单 | 同上 | 同上 |
| 3 | `设计文档-工单16.md:349`、`进度记录.md:243/253` | `svg-face`（纯代码 SVG）作本地实现 | **卡通脸 09-18 已退役**，现为 `photo` / `live2d` |

- [ ] **2.1** 改这 3 处（`CLAUDE.md:148` 已经写对，作为改法参照）
- [ ] **2.2** 复核：`grep -rn "AvatarStage" .` 与 `grep -rn "svg-face"` 只剩"已退役"标注行

---

## 五、P1 · 演示动线人走一遍（约 30 分钟，**这条只有你能做**）

DoD 第 1 条「可现场演示」的最终确认权在你。服务已起，建议动线：

- [ ] 登录 `demo_teacher` / `demo123456` → 智能备课：生成教案（看逐字流式）→ 编辑 → 保存 → 导出
- [ ] 智能助教 → 智能问答：问「表格里 Adam 优化器适合什么场景？」→ 看数字人**边生成边念**、口型开合
- [ ] 切形象（小满 = Live2D 档）：听萌音（+8% 语速 / +30Hz 音调）、看口型是否稳定
- [ ] 虚拟教室 `/lecture`：播示例课，看翻页与字幕是否对齐
- [ ] **断网风险**：Edge-TTS 走微软在线服务，演示前确认有网（断网不报错，只是不朗读）

有问题当场记下来，我按记录修。

---

## 六、P2 · 有余力再做

- [ ] **3.1** 学生账号侧边栏「我的备课」→ 点进去 403（`App.vue:20-22` 没按角色隐藏菜单）。修法：菜单项加角色判断
- [ ] **3.2** 仓库杂物：`frontend/dist-verify/`（≈17MB，已 gitignore）、空目录 `frontend/src/views/interview/`、孤儿组件 `views/Placeholder.vue`（全仓零引用）
- [ ] **3.3** 形象图压缩：6 张 PNG 共 **14MB**（5×2.36MB + 2.0MB），可压到 600KB 内 —— ⚠️ **有观感回归风险**，必须先出对比样张给你看，别直接覆盖原图

---

## 七、需要你拍板的两件事（不拍板就卡住）

1. **声音克隆走哪条路？**（09-21 已调研完，结论待定）
   - 本地 **GPT-SoVITS**：CPU 能跑但慢 → 只适合**成课离线出片**，实时问答跟不上
   - **云端 API 克隆**（阿里百炼 CosyVoice / Azure 自定义语音 / 腾讯云）：实时可用，按月付费
   - 合规：仅克隆本人或获授权声音，对外发布需标识
2. **阶段三（云端数字人）要不要先做 0 元 POC？**
   - 首选硅基智能 **DUIX H5 SDK**，**免费档 5 分钟/天**，够跑通 POC
   - 正式用 Creator 档约 **$29~59/月**（200~420 元），定制自有形象约 9800 元/套（后置）
   - 详见 `docs/research/云端数字人API选型调研.md`

---

## 八、今日明确不做（避免跑偏）

- ❌ 阶段三**写代码**：需商务采购落地，不是能自己开工的活
- ❌ Wav2Lip 截帧回填（让实时问答口型真开口）：大工程，单独立项
- ❌ 动 `data/edu_agent.db` 演示数据、重跑 seed、清库
- ❌ push 到 gitee（按你的拍板，只做本地 commit）

---

## 九、收工标准

1. `docs/evidence/工单20|21|22/` 三个目录有图、断言全绿
2. `grep AvatarStage` / `grep svg-face` 无残留错误描述
3. pytest 仍 **324 passed**（无回归）
4. 演示库还原到位（`attempts=4` / `mistake_book id=1 pending` / `questions` 变式题 0 条）
5. 变更全部本地 commit（带 `[工单XX]` 前缀），`git status` 干净
