# [工单17] 人工智能NLP-Agent数字人项目-教育智能体 —— 后端依赖
# 安装：pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# 注意：本机无独立显卡，torch 必须为 CPU 版，禁止拉取 CUDA 版

# --- Web 框架 ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- 数据库 ---
SQLAlchemy>=2.0

# --- 配置 ---
pydantic>=2.7
pydantic-settings>=2.3

# --- 认证 ---
PyJWT>=2.8
bcrypt>=4.1

# --- LLM（OpenAI 兼容接口：DeepSeek / Qwen） ---
openai>=1.40

# --- 文档解析与导出 ---
python-docx>=1.1
python-pptx>=0.6.23
openpyxl>=3.1
PyMuPDF>=1.24

# --- RAG（工单18） ---
chromadb>=0.5
rank-bm25>=0.2.2
jieba>=0.42

# --- ASR（工单20，纯 CPU） ---
faster-whisper>=1.0

# --- 测试 ---
pytest>=8.0
httpx>=0.27
